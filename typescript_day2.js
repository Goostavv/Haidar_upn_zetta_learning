function detec(union) {
    var merge;
    merge = union.join(" ");
    console.log(merge);
    return merge;
}
detec([1, 'data', '3', 'result']);
detec(['Bejo', 'has', '4', 'sport', 'car']);
function detach(pg) {
    return console.log(pg);
}
detach(["MaN", 9]);
